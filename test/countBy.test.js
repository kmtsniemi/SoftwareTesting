import chai from "chai"
import countBy from "../src/countBy.js"
const expect = chai.expect


describe("CountBy", () => {
/*   
 it("Joukossa on kahta erilaista property-arvoa", () =>{
    const collection = [
        {'kategoria': 'Pakasteet'},
        {'kategoria': 'Leivät'},
        {'kategoria': 'Pakasteet'}]
    const result = {'Pakasteet': 2, 'Leivät':1};
    expect(countBy(collection, value => value.kategoria)).to.equal(result)
 });

 it("Joukon alkiolta puuttuu haluttu property", () =>{
    const collection = [
        {},
        {'kategoria': 'Leivät'},
        {'kategoria': 'Pakasteet'}]
    const result = {'Pakasteet': 1, 'Leivät':1};
    expect(function() {countBy(collection, value => value.kategoria)}).to.equal(result)
 });

 it("Propertyn arvo on null", () =>{
    const collection = [
        {'kategoria': 'Pakasteet'},
        {'kategoria': null},
        {'kategoria': 'Pakasteet'}]
    const result = {'Pakasteet': 2, null : 1};
    expect(countBy(collection, value => value.kategoria)).to.equal(result)
 });

 it("Joukko on null-arvoinen", () =>{
    const collection = null
    expect(function() {countBy(collection, value => value.kategoria)}).to.throw()
 });
*/ 
})